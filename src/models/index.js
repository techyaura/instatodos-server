const UserModel = require('./user.model');
const TodoModel = require('./todo.model');
const TodoLabelModel = require('./todo-label.model');
const ThoughtModel = require('./thought.model');

module.exports = {
  UserModel,
  TodoModel,
  TodoLabelModel,
  ThoughtModel
};
