# Mutations
extend type Mutation {
  googleLogin(input: GoogleInputType!): UserLoginSuccessType!
}

# Field Types
input GoogleInputType {
  firstname: String!,
  lastname: String,
  email: String!,
  gID: String!,
  profilePic: String!
}
