# Queries -------------------------------------------

type Query {
  setting : SettingType!
}

# Mutations -------------------------------------------

type Mutation {
  setting(input: SettingInputType!): SettingType!
}

#Types ----------------------------------------------

input SettingInputType {
  theme: String,
  lang: String
}

type SettingType {
  theme: String,
  lang: String
}
